{
  "name": "dropi-product-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python -m http.server 8000 --bind 0.0.0.0",
    "check": "node --check app.js",
    "test": "node --test"
  }
}
